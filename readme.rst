===========
MAC Spoofer
===========

Run this ZSH script to extend your $1 coffee free wifi duration.
Edit the network interface if the address doesn't change.

To execute this script, run ``sudo zsh script.zsh`` command.
